合同授予（包税权投票、工程指派）---- 合同拍卖在广场阶段已经实现了，但是需要对流程进行更新：
1. 广场阶段生成可用的合同（根据行政区合同的执行情况更新）进入需求池。
2. 这些合同需求在元老院阶段提交给元老院表决，通过预算（设定包税低价或工程预算）；
3. 获得预算的合同在下个回合的广场阶段进入拍卖池拍卖；

针对你的开发思路，我做如下澄清：
1. EXPIRED：过期（如未在元老院通过或无人竞标）---- 如果预算未通过，则保留在PENDING状态；如果未拍卖，则保留在BUDGET状态；
包税权合同EXPIRED后，会继续执行，直到新的包税权合同取代。在包税权合同到期前1年的广场阶段，需求池会自动生成一个新的包税权合同（PENDING）。这个新合同走完预算审批后，第二年拍卖成功再取代旧有的过期合同。这样可以确保国家税收不遗漏。
在 Contract 实体中添加布尔字段 _is_extended，默认 False。当合同达到年限后，在税收阶段将其标记为延期。延期合同仍然有效，并继续产生收益，但显示为“（已延期）”
当新合同中标后，删除该延期合同，根据新合同设定税率和受益人。
这样，玩家通过 contracts 命令能清晰看到哪些合同仍在正常执行，哪些已延期。
2. 工程合同完成后，会进入保质期(Y)。在保质期到期前N+1年（N是该工程完成的周期, Y>N+1）的广场阶段，需求池会自动生成一个新的工程合同（PENDING）。这个新合同走完预算审批后，第二年拍卖成功就可以开始执行了。


2. 元老院阶段，在立法子阶段（整个元老院阶段包含：宣战/外交-->任命总督-->审判总督，最后才是立法子阶段），如果执政官将所有合同预算审批列入立法表决项目（决策树，随机），则针对每个合同进行表决，表决采用决策器和功能代码分离（决策树，随机表决）。

3. 保留原有合同授予逻辑的代码，但暂时注释掉，以便未来扩展。---- 可以删除，因为合同都是走拍卖流程。

4. 竞标逻辑迁移----竞标保留在广场阶段，不用迁移，但是要和新流程适配（只针对预算审批通过的竞标）

5.如果元老院阶段未通过某些合同（例如因国库不足），可将其保留为PENDING 到下一年。

请确认你已理解，并等我确认再开始编码。

针对你的反馈，我做如下澄清：

1.立法子阶段中，执政官将所有待决合同（PENDING）作为法案提交，每个合同单独表决。--- 并非每个合同，此处应使用决策器，随机挑选合同上会表决，模拟执政官/HARO对大会议程的控制权。

2.EXPIRED：不再使用（由新合同取代）。---- EXPIRED可能表述有误，可以换为EXTENDED，EXTENDED包税权合同将继续有效，直到新的包税权合同拍卖成功并生效。

请确认你已理解，并等我确认再开始编码。


03:24 测试结果问题分析

1. 收入阶段私地收益总净收入与任务私地信息的总收入不匹配；
2. 7 new contract(s) announced --- 需要增加有预算的合同信息打印；
3. Pending Contracts 里面需要删除 senate Phase的描述，已经过时；
4. contracts命令只打印了pending的合同，其他状态的合同都不可见。
5. 撒丁尼亚包税权（BC280年，BC279年，BC278年）流拍不合理，有骑士出价，高于底价，为何流拍？科西嘉包税权(BC279年，BC278D年)流拍，都有出价，为何流拍？
6. 竞标的合同，没有打印竞标结果。


你是EOR 主程序员DS005，请查收EOR项目交接文档（含架构师项目指引）。请明确告知你已经全面掌握了EOR项目目前的进展。你确认后，我将分2批次，将目前的基线代码提供给你，由你继续后续内容的开发。


请查收第一批文件，确认后我再发送第二批.




新增功能 --- 宣战
1. 广场阶段出现的战争威胁进入一个未激活战争池；
2. 元老院提案阶段，执政官提议直接宣战（决策器随机决策）
3. 元老院表决阶段，执政官所属派系直接支持，其他2个派系随机（决策器）
4. 表决结果：通过，战争直接进入激活态；否决，战争威胁状态不变；
5. 如果存在多个战争威胁，针对每一个威胁走一遍步骤2-4。
6. 有战争进入激活状态，解锁“战斗阶段”，如果没有激活的战争，“战斗阶段”的指令不可操作（召集军团，指派将领，出征作战-生成战斗结果:大胜/小胜/延宕/小败/大败）；---- 在战斗阶段开发。
7. 一旦元老院表决进入战争状态（或广场阶段，战争已经被激活），执政官即进入出征模式，后续元老院议程根据HARO序列交接给下级官员主持。且执政官的影响力将不参与元老院表决。
8. 一旦新的执政官产生，仍处于战区的前执政官将更新为Proconsul,直到返回罗马放弃军权。

影响力计算 ----- 元老院表决，只计算元老的影响力，骑士和平民的影响力不计入。

更新派系领袖：调用 faction.update_faction_leader(state)，新领袖自动成为留在罗马的影响力最高者（通常为 censor）。---- 派系领袖不用更新，只需要更新HARO主持人，用于主持执政官离开罗马后的元老院议事（挑选法案）。
国库扣除宣战费用正常。--- 军费在战争阶段和收入阶段自动结算，不需要在元老院阶段决算。但是执政官需要在元老院提出出兵规模，这个直接影响费用和预算审批，也是元老院表决是否支持开战的主要信息。


需要确保前期合同投票的元老院计票也只考虑元老。
self.budget_decider = AutoBudgetDecider()--- 这个留下来干嘛？


广场的新人物，必须在人口阶段开始之前被清理掉，避免干扰选举；




