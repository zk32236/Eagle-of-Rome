小胜后降级为威胁已实现，但战败（DEFEAT）后战争仍应保持活跃，允许重新集结再战，目前被标记为失败并从列表中移除。需引入 BROKEN 或 ROUTED 状态。---- 请检查最近2日的开发报告和最新的代码，这个问题应该已经解决。


DS008的工作交接给DS009了，请根据最新的DS008 开发报告（3月2日），参考你之前写的《EOR MVP 0.5 项目指引》输出一份新的EOR MVP 0.5 项目指引给新任主程序员 DS009，引导他继续完成。


请查看一下需求澄清：

1. 时间点：元老院阶段（Senate Phase）结束后立即执行。---- 任命总督是元老院阶段仅次于宣战以后之后的第二项议程。因此是在元老院阶段中间，而不是结束后。


2. 抽签规则：按卸任时间倒序（新卸任的优先），依次分配给对应类型的行省，直到行省满额或候选人用尽。--- 这里应该有一个抽签的过程。例如如果有2个执政官行省（A,B），3个合格候选人；那么最近卸任的执政官,可以先抽（这里是随机，A或B），然后下一个前执政官抽（因为只剩1个，直接拿B），第三位前执政官则没有行省可抽取了。

3. 离任与返回：

新总督任命后立即离开罗马（is_absent = True），其影响力不再计入后续的元老院表决和下一回合的公民大会竞选。（此刻新任总督只是获得任命，但是还没有到行省履职，可以理解为在途）

旧总督在决算阶段（Resolution Phase） 返回罗马（is_absent = False），可以重新下一回合的大选和表决。（此刻，两位总督进行交替，总督标签从旧总督转移到新总督，因此不应该出现一个行省同时有2个总督的情况）。


4. 在实现过程中注意添加日志信息（INFO和DEBUG）和美化抽签结果在cli输出打印（简单明了美观）。

5. 候选人条件：
存活，官职，当前在罗马（is_absent=False）。

6.从 office_history 中获取最近一次担任该官职的卸任回合（end_turn），取最大值。若从未担任过该官职，则不计入。---- 注意游戏是公元前纪年BC280比BC279靠前。BC279的前执政官的抽签优先于BC280的前执政官；如果使用-280和-279,也可以说数值大的先抽（-279>-280）。

7. 游戏初始化有3个行省：西西里（proconsul行省），科西嘉和撒丁岛是（Propraetor行省）。

请根据以上反馈内容调整你的开发方案。

请查看以下需求澄清：

1. 请在代码中确认 "ex-consul" 或 "ex-praetor"并非官职，因为不在office_rank": {
      "dictator": 6,
      "censor": 4,
      "consul": 5,
      "praetor": 3,
      "tribune": 1,
      "quaestor": 2
    }, 的序列中。

2. 保民官是在所有议程表决结束后行使否决权，因此在任命总督时，不可能打印是否否决的结果。
是否否决应在元老院阶段最后环节保民官否决权时打印。

3. 如果新任命的总督被保民官否决，请注意清理掉其新任总督标签。且其is_absent标签应该在保民官通过任命后再修改为true。

请根据以上反馈内容细化你的开发方案。

问题反馈：

1. 现任执政官Gaius · Fabius · Brutus 和现任大法官Aulus · Valerius · Asiaticus为何去抽签当总督了？他们不应该参加抽签。
2. 行省总督任命的打印里，年份显示不正确。
3. 交接阶段没有看到新旧总督换岗的打印信息
4. 行省信息打印中并没有总督的信息和行省类型（Proconsul/Propraetor）


请提供print_assignments的完整函数


问题反馈：
1. 初始化行省总督应该不在罗马（is_absent = true）为什么还会参选执政官和大法官？
2. ex-consul和ex-praetor不是官职，但是proconsul和propraetor是官职，sf时应该显示官职
3. 打印行省信息，为何所有行省都是proconsul类型？为什么总督propraetor却是ex-praetor？
4. sf具体人物时，请显示他是否在罗马


问题反馈：

1. 为何Gaius · Veturius · Maximus被上任为总督，同年当选监察官，但是在元老院和决算阶段都没有打印其选为总督和交接的信息，而且还不在罗马。
2. 为何撒丁尼亚没有初始化总督？

问题反馈：
1. 初始化的总督没有更新官职。
2. Decimus · Domitius · Cicero虽然是总督不在罗马，但是选举时仍选了他做censor，Decimus · Domitius · Cicero也是总督，但是同样当选了执政官。请检查选举的候选人是否检查了is_absent参数。


问题反馈：
1. 为何西西里没有选出总督，是没有候选人吗？“无行省需要任命” --- 是什么意思？
2. 为何只有撒丁尼亚抽了新总督，科西嘉呢？为什么任何打印都没有？
3. 为何Aulus · Domitius · Cicero还没有到决算阶段，就把前任总督Numerius · Plautius · Cicero 换下来了。这个交换应该在决算阶段啊


问题反馈：
1. Gnaeus · Sulpicius · Bibulus为什么可以选2个行省当总督？
2. 撒丁岛没有任命总督，但是也没有任何打印为何没有任命总督。建议增加打印信息，提示没有合格的候选人，现任总督留任一年。
3. 西西里是proconsul行省，为什么官职给Gnaeus · Sulpicius · Bibulus仍是propraetor?

问题反馈：
1. 第二回合显示3个行省没有合格人选，但是检查人物信息，至少有2位元老没有官职，但是满足总督条件。Decimus · Sempronius · Caesar， Aulus · Plautius · Maximus。请定位





