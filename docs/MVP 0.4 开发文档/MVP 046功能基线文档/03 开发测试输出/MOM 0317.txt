1. UI 界面和设计不完全一致；---- 解决
2. 庆典阶段只需要展示候选人的ID,姓名，派系，和属性（军略，智略，热忱，魅力），不需要展示其影响力； ---- 解决
3. 候选人一次只能竞选一个职位，一般应该只竞选较高的职位； --- 未完全解决
4. 已经担任过consul的不会再竞选praetor，已经担任过praetor的不会再竞选quaestor；
5. 玩家输入指令后，在打印输出执行结果后，应该直接进入下一次输入提示符；
6. 当前玩家输入提示符是红色的，这是否意味着输入不是调用的API函数？是否符合设计规范？
7. "auto_forum": true 也控制了人口阶段，需要一个独立的控制器。