指令1接收人：KIMI（架构师）
指令主题：细化EOR Command命令体系架构设计文档
【核心聚焦】：仅围绕Command命令体系的「自动注册+接口约束」设计，暂不考虑GameState/Config相关逻辑；仅优化结构，不修改原有命令的业务逻辑，符合渐进式优化要求。
核心输入：
1. 基线代码：debug_cli.py（仅提取原有命令耦合逻辑片段）；
2. 参考文档：《EOR_接口规范说明书》- Command接口章节（仅2页核心内容）；
3. 核心约束：Pythonic原则、扁平化目录（commands目录无下级子目录）。
核心任务（仅限以下3项）：
1. 明确Command抽象基类的必选属性/方法清单（name/aliases/description/execute）及接口约束；
2. 设计CommandRegistry自动扫描规则（扫描目录、文件过滤、重复命令处理），输出核心伪代码（≤20行）；
3. 识别自动注册的技术风险点（循环导入/命令名冲突）及规避方案（各1条核心方案即可）。
交付物（严控篇幅）：
1. 《EOR Command命令体系架构设计细化文档_V1.1》（Markdown格式，≤5页）；
2. CommandRegistry自动注册伪代码片段（单独txt文件，标注关键步骤注释）。
验收标准（仅验证当前任务）：
1. 伪代码逻辑可直接指导DeepSeek编写代码，无模糊表述；
2. 覆盖自动注册核心场景（重复命令、文件导入失败）；
3. 符合Pythonic原则，无过度封装设计。

指令2接收人：KIMI（架构师）
指令主题：细化EOR GameState状态管理架构设计文档
【核心聚焦】：仅围绕GameState的「单例移除+多实例隔离」设计，暂不考虑Command/Config相关逻辑；保留原有公共方法，仅做渐进式优化。
核心输入：
1. 基线代码：game_state.py（完整文件，标注原有单例实现逻辑）；
2. 参考文档：《EOR_开发指南》- 命名规范+扁平化目录章节（仅1页）；
3. 核心约束：支持多实例独立创建、测试专用实例、状态重置。
核心任务（仅限以下3项）：
1. 明确GameState实例化规则（显式创建、配置注入、测试实例创建逻辑）；
2. 输出移除单例的核心伪代码（≤20行），标注多实例隔离关键节点；
3. 说明多实例隔离的验证方法（供DeepSeek编写测试用例参考，1-2条即可）。
交付物（严控篇幅）：
1. 《EOR GameState状态管理架构设计细化文档_V1.1》（Markdown格式，≤4页）；
2. GameState单例移除伪代码片段（单独txt文件）。
验收标准（仅验证当前任务）：
1. 伪代码明确移除单例的具体方案，无模糊表述；
2. 多实例隔离逻辑可验证，符合Pythonic原则；
3. 保留原有公共方法，无破坏性修改。

【身份：EOR项目架构师】
指令接收人：DeepSeek（架构师）
指令主题：细化EOR Config配置管理架构设计文档
【核心聚焦】：仅围绕Config的「默认值回退+点号路径访问」设计，暂不考虑Command/GameState相关逻辑；不修改原有配置读取核心逻辑。
核心输入：
1. 基线代码：config.py（完整文件，标注原有配置读取逻辑）；
2. 参考文档：《EOR_测试验收标准》- 配置加载要求章节（仅1页）；
3. 核心约束：默认值回退、点号路径访问、运行时重载；设计文档需包含「代码映射说明」，明确伪代码对应最终代码的文件/方法名。
核心任务（仅限以下3项）：
1. 明确配置加载优先级（配置文件>内置默认值）及回退触发条件；
2. 输出点号路径解析逻辑伪代码（≤15行）；
3. 识别配置文件异常（格式错误/权限不足）的处理方案（1条核心方案即可）。
交付物（严控篇幅）：
1. 《EOR Config配置管理架构设计细化文档_V1.2》（Markdown格式，≤4页）；
2. Config点号路径解析伪代码片段（单独txt文件）；
3. 代码映射说明（1页，标注伪代码对应config.py的具体方法名）。
验收标准（仅验证当前任务）：
1. 伪代码覆盖配置加载/访问核心场景，可落地；
2. 默认值回退逻辑符合验收标准要求；
3. 代码映射说明清晰，无模糊对应关系。


指令4接收人：DeepSeek（架构师）
指令主题：制定EOR MVP 0.4.5代码审查规则清单
【核心聚焦】：仅围绕「Command/GameState/Config」三个核心模块制定审查规则，暂不涉及其他业务模块；规则需可落地、可验证，不写抽象条款。
核心输入：
1. 已输出的3份架构设计细化文档；
2. 参考文档：《EOR_开发指南》- 编码规范章节（仅1页）；
3. 核心约束：覆盖CLI拆分、单例移除、配置优化核心整改目标。
核心任务（仅限以下2项）：
1. 按模块维度制定审查Checklist（每个模块≤5条检查项），每条明确「检查方法+合格标准+不合格示例」；
2. 通用规则仅保留Pythonic原则、命名规范、异常处理3条核心项。
交付物（严控篇幅）：
《EOR MVP 0.4.5代码审查Checklist_V1.0》（Markdown格式，≤3页）。
验收标准（仅验证当前任务）：
1. 检查项覆盖三个核心模块的整改目标，无遗漏；
2. 检查方法可落地（如「全局搜索GameState类，检查无__new__单例实现」）；
3. 不合格示例贴合项目实际场景。

指令5接收人：DeepSeek（架构师）
指令主题：输出EOR核心模块单元测试用例模板
【核心聚焦】：仅输出「Command/GameState/Config」三个模块的测试模板，基于pytest框架，仅提供模板框架，不编写具体业务测试代码。
核心输入：
1. 已输出的3份架构设计细化文档；
2. 参考文档：《EOR_测试验收标准》- 单元测试要求章节（仅1页）；
3. 核心约束：覆盖核心验收点（单例移除、配置默认回退、自动注册）。
核心任务（仅限以下2项）：
1. 为每个模块输出测试模板（每个模板≤100行），包含「测试前置/后置+核心断言点+运行命令」；
2. 输出模板使用说明（≤1页），标注适配不同模块的修改要点。
交付物（严控篇幅）：
1. 《EOR核心模块单元测试用例模板_V1.0》（Markdown格式，≤3页）；
2. 模板使用说明文档（≤1页）。
验收标准（仅验证当前任务）：
1. 模板覆盖三个模块的核心验收点；
2. 模板可直接复用，仅需修改少量参数；
3. 包含覆盖率统计方法，符合测试验收标准。

指令6接收人：DeepSeek（架构师）
指令主题：审查DeepSeek编写的CommandRegistry核心代码
【核心聚焦】：仅审查CommandRegistry代码的「自动扫描逻辑+接口合规性」，暂不审查其他模块代码；仅输出具体整改建议，不编写代码。
核心输入：
1. DeepSeek交付物：sys_registry.py、test_sys_registry.py；
2. 参考文档：指令1输出的Command架构设计文档、指令4输出的审查Checklist；
3. 核心约束：仅验证设计方案落地性、测试覆盖率≥90%。
核心任务（仅限以下2项）：
1. 按设计文档和审查规则逐项审查，标注「合格项/不合格项+具体行号」；
2. 对不合格项输出具体整改建议（含代码片段示例，每条≤10行）。
交付物（严控篇幅）：
《CommandRegistry代码审查报告_V1.0》（Markdown格式，≤3页）。
验收标准（仅验证当前任务）：
1. 审查覆盖自动注册核心设计要求，无遗漏；
2. 不合格项标注具体行号，整改建议可直接落地；
3. 验证单元测试有效性，指出覆盖不足的场景。

指令7接收人：DeepSeek（架构师）
指令主题：输出EOR MVP 0.4.5最终验收验证步骤手册
【核心聚焦】：仅输出「单元测试+集成测试+核心验收点」的验证步骤，暂不涉及验收组织/管理类内容；步骤需可操作、可落地。
核心输入：
1. 已输出的架构设计文档、审查规则、测试模板；
2. 参考文档：《EOR_测试验收标准》- 核心验收点章节（仅2页）；
3. 核心约束：覆盖单例移除、配置默认回退、CLI不崩溃核心验收点。
核心任务（仅限以下2项）：
1. 按「环境准备→单元测试→集成测试」拆解步骤，每个步骤标注「操作命令+预期结果」；
2. 核心验收点逐项标注验证方法（每个验收点≤2步操作）。
交付物（严控篇幅）：
《EOR MVP 0.4.5验收验证步骤手册_V1.0》（Markdown格式，≤5页）。
验收标准（仅验证当前任务）：
1. 步骤可落地，零基础人员可按手册完成验证；
2. 覆盖所有核心验收点，包含常见问题排查方法；
3. 操作命令可直接复制执行，无模糊表述。
