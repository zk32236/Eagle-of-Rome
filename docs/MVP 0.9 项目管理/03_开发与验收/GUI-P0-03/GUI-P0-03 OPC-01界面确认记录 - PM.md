# GUI-P0-03 OPC-01界面确认记录 - PM

- 日期：2026-07-05
- 确认人：项目负责人
- 记录人：项目经理 PM
- 对应任务：GUI-P0-03 OPC-01 GUI重设计界面确认与实施拆分
- 当前结论：界面确认门禁通过，批准 SA 编写并发布 `GUI-P0-03A OPC主壳骨架落地` 技术任务书给 CGT-01。

## 一、确认结论

项目负责人已全部批准 SA-01 提出的界面确认项。

`OPC-01 v3.23` 确认为后续 GUI 开发的目标界面方向。后续如需微调，应在 `EOR_GUI设计文档.md` V2.0 范围内迭代，不要求每一个小版本重新走 PM 审批。

## 二、项目负责人确认意见

> V3.23 完整覆盖了 7 阶段交互模式，后续 GUI 开发以此为准。如需微调在设计文档 V2.0 范围内迭代，不必每版都走审批。
>
> 五区布局（顶栏/左导航/中栏/右面板/底栏）+ 阶段容器 + 公告区 + 状态日志 + 底部按钮占位，不接复杂业务。这正好是 V3.23 的 HTML 结构层级，CODEX 可以按原型 HTML 的 DOM 树直接映射。
>
> 第一轮允许「已接入 / 只读 / 占位」混合。优先级建议：游戏状态 > 派系信息 > 战争列表 > 军团状态 > 其余占位（前四者 API 已有现成数据）。
>
> 关键约束：所有新增 UI 文案必须写 GuiText.get("key") 或类似集中调用，不允许任何行内字符串硬编码。这样 P3 加英文包时只需加一个 JSON 文件，不需要改代码。

## 三、PM 授权范围

SA 可进入下一步：

- 定稿 `GUI-P0-03A OPC主壳骨架落地` 技术开发任务书。
- 将 `GUI-P0-03A` 发布给 CGT-01。
- 任务范围限定为主壳骨架、五区布局、阶段容器、公告区、右侧状态/日志、底部按钮占位与前四个只读查询入口优先级规划。

## 四、GUI-P0-03A 范围边界

### 允许

- 按 OPC-01 v3.23 HTML 原型的 DOM 层级映射 QML 主壳结构。
- 实现五区布局：顶栏、左导航、中栏、右面板、底栏。
- 建立阶段容器、公告区、状态日志、底部按钮占位。
- 底部 12 查询按钮第一轮允许混合状态：已接入、只读、占位。
- 第一轮查询入口优先级为：游戏状态、派系信息、战争列表、军团状态，其余占位。
- 所有新增 UI 文案必须通过 `GuiText.get("key")` 或等价集中调用，不允许行内字符串硬编码。

### 禁止

- 不得一次性接入元老院、广场、战争、收入和决算的复杂业务动作。
- 不得把 HTML 原型代码直接迁入 QML。
- 不得让 GUI 直接调用 CLI Command、`game_api.execute_phase()` 或 `CombatCommand`。
- 不得绕过 API/SessionStore 权限边界展示私有信息。
- 不得引入 P1 新玩法或修改 Core 规则。

## 五、后续节奏

推荐后续顺序保持 SA 拆分：

1. `GUI-P0-03A OPC主壳骨架落地`
2. `GUI-P0-03B GuiApiAdapter 第一批增量与全局只读查询`
3. `GUI-P0-03C/D 元老院交互化`
4. `GUI-P0-03E 广场视图闭环`
5. `GUI-P0-03F 战争/海战 GUI 边界与执行`
6. `GUI-P0-03G 收入与决算优化`
7. `GUI-P0-03H i18n 完整语言包、多玩家最终调试`